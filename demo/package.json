{
  "name": "@trovec/demo",
  "version": "2.0.0",
  "private": true,
  "description": "Interactive CLI demo for Trovec",
  "type": "module",
  "scripts": {
    "start": "npx tsx src/main.ts",
    "build": "echo 'no build needed'",
    "clean": "echo 'nothing to clean'"
  },
  "dependencies": {
    "@trovec/core": "*",
    "@trovec/embedder-local": "*",
    "@trovec/embedder-openai": "*",
    "@trovec/embedder-ollama": "*"
  },
  "devDependencies": {
    "tsx": "^4.0.0"
  }
}
