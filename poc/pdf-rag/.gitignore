# Generated by the benchmark script (poc/pdf-rag/src/benchmark.ts).
# Regenerate with: npm run benchmark
bench-queries.json
BENCHMARK_RESULTS.json

# Auto-fetched ONNX weights for benchmark-only models. Cached per-machine.
# Tracked: src/bench/checksums.json (for deterministic verification).
bench-models/

# Benchmark stdout/stderr capture from `npm run benchmark`.
benchmark-v3.log
