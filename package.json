{
  "name": "trovec",
  "private": true,
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "build": "npm run build -w packages/core -w packages/embedder-local -w packages/embedder-openai -w packages/embedder-ollama -w packages/vscode-trovec -w packages/cli -w demo",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "npm run clean --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
